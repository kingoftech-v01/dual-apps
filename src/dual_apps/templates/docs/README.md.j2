# {{ app_name_title }}

**{{ app_name_title }}** - Django app with dual-layer architecture (Frontend + API)

Generated by [dual-apps](https://github.com/dual-apps/dual-apps) v{{ version }}

## Quick Start

```bash
# Install
pip install -e .

# Add to Django project
# settings.py: INSTALLED_APPS += ['{{ app_name }}.apps.{{ app_name_pascal }}Config']
# urls.py: path('{{ app_name }}/', include('{{ app_name }}.urls'))

# Migrate
python manage.py migrate {{ app_name }}

# Create demo data
python manage.py demo_{{ app_name }}

# Run tests
pytest
```

## URLs

### Frontend (HTMX Templates)
| URL | Name | Description |
|-----|------|-------------|
| `/{{ app_name }}/` | `{{ model_name_snake }}_list` | List all {{ model_name_plural }} |
| `/{{ app_name }}/create/` | `{{ model_name_snake }}_create` | Create form |
| `/{{ app_name }}/<uuid:pk>/` | `{{ model_name_snake }}_detail` | Detail view |
| `/{{ app_name }}/<uuid:pk>/edit/` | `{{ model_name_snake }}_edit` | Edit form |

### API (DRF JSON)
| Method | URL | Description |
|--------|-----|-------------|
| GET | `/api/v1/{{ app_name }}/{{ model_name_kebab }}s/` | List all |
| POST | `/api/v1/{{ app_name }}/{{ model_name_kebab }}s/` | Create new |
| GET | `/api/v1/{{ app_name }}/{{ model_name_kebab }}s/{id}/` | Retrieve |
| PUT | `/api/v1/{{ app_name }}/{{ model_name_kebab }}s/{id}/` | Update |
| DELETE | `/api/v1/{{ app_name }}/{{ model_name_kebab }}s/{id}/` | Delete |
| POST | `/api/v1/{{ app_name }}/{{ model_name_kebab }}s/{id}/archive/` | Archive |

## Features

- **Dual Layer Architecture**: Frontend (HTMX) + API (DRF)
- **UUID Primary Keys**: Secure, distributed-friendly
- **Owner-based Permissions**: IsOwnerOrReadOnly
- **88% Test Coverage**: 45+ tests included
- **HTMX Integration**: Inline editing, infinite scroll
- **Tailwind CSS**: Modern, responsive design

## Project Structure

```
{{ app_name }}/
├── {{ app_name }}/           # Django app
│   ├── models.py            # {{ model_name }} model
│   ├── views_frontend.py    # HTMX views
│   ├── views_api.py         # DRF ViewSet
│   ├── serializers.py       # DRF serializers
│   ├── urls.py              # Dual URL patterns
│   └── permissions.py       # Custom permissions
├── templates/{{ app_name }}/ # HTML templates
├── static/{{ app_name }}/    # CSS/JS
├── tests/                   # 45+ tests
└── docs/                    # Documentation
```

## License

MIT License - {{ year }}
