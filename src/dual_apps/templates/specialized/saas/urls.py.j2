"""
SaaS URL configuration for {{ app_name }} app.
Generated by dual-apps v{{ version }}
"""

from django.urls import path
from . import views_frontend

app_name = "{{ app_name }}"

urlpatterns = [
    path("", views_frontend.organization_list, name="list"),
    path("organizations/", views_frontend.organization_list, name="organization_list"),
    path("organizations/create/", views_frontend.organization_create, name="organization_create"),
    path("org/<slug:slug>/", views_frontend.organization_detail, name="organization_detail"),
    path("org/<slug:slug>/members/", views_frontend.organization_members, name="organization_members"),
    path("org/<slug:slug>/billing/", views_frontend.organization_billing, name="organization_billing"),
    path("pricing/", views_frontend.pricing, name="pricing"),
]
