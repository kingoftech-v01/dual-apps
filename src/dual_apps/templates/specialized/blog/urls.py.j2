"""
Blog URL configuration for {{ app_name }} app.

Frontend routes for the blog.

Generated by dual-apps v{{ version }}
"""

from django.urls import path
from . import views_frontend

app_name = "{{ app_name }}"

urlpatterns = [
    # Post list (home)
    path("", views_frontend.post_list, name="list"),
    path("posts/", views_frontend.post_list, name="post_list"),

    # Post detail
    path("post/<slug:slug>/", views_frontend.post_detail, name="post_detail"),

    # Categories
    path("category/<slug:slug>/", views_frontend.category_list, name="category_list"),

    # Actions
    path("post/<slug:slug>/comment/", views_frontend.add_comment, name="add_comment"),
    path("post/<slug:slug>/like/", views_frontend.like_post, name="like_post"),
]
