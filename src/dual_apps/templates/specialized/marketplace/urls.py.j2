"""
URL configuration for marketplace app.
Generated by dual-apps v{{ version }} - Marketplace Template
"""

from django.urls import path, include
from rest_framework.routers import DefaultRouter

from . import views_api

app_name = 'vendors'

# API Router
router = DefaultRouter()
router.register(r'vendors', views_api.VendorsViewSet, basename='vendor')
router.register(r'products', views_api.VendorProductViewSet, basename='product')
router.register(r'orders', views_api.VendorOrderViewSet, basename='order')
router.register(r'payouts', views_api.PayoutViewSet, basename='payout')

urlpatterns = [
    path('api/', include(router.urls)),
]
