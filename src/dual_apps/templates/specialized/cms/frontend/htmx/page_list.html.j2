{% raw %}{% extends "cms/base_cms.html" %}

{% block title %}Pages - {{ project_name_display }}{% endblock %}

{% block cms_content %}
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">Pages</h1>
    <a href="{% url 'cms:page_create' %}" class="btn btn-primary">
        <i class="bi bi-plus-lg me-1"></i> New Page
    </a>
</div>

<!-- Filters -->
<div class="card mb-4">
    <div class="card-body">
        <form hx-get="{% url 'cms:pages' %}"
              hx-target="#pages-table"
              hx-swap="innerHTML"
              hx-trigger="change from:.form-select, input from:#search-input delay:300ms"
              hx-push-url="true">
            <div class="row g-3">
                <div class="col-md-4">
                    <input type="text" name="search" id="search-input" class="form-control"
                           placeholder="Search pages..." value="{{ request.GET.search }}">
                </div>
                <div class="col-md-3">
                    <select name="status" class="form-select">
                        <option value="">All Status</option>
                        <option value="published" {% if request.GET.status == 'published' %}selected{% endif %}>Published</option>
                        <option value="draft" {% if request.GET.status == 'draft' %}selected{% endif %}>Draft</option>
                        <option value="scheduled" {% if request.GET.status == 'scheduled' %}selected{% endif %}>Scheduled</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <select name="author" class="form-select">
                        <option value="">All Authors</option>
                        {% for author in authors %}
                        <option value="{{ author.pk }}" {% if request.GET.author == author.pk|stringformat:"s" %}selected{% endif %}>
                            {{ author.get_full_name }}
                        </option>
                        {% endfor %}
                    </select>
                </div>
                <div class="col-md-2">
                    <button type="button" class="btn btn-outline-secondary w-100"
                            hx-get="{% url 'cms:pages' %}"
                            hx-target="#pages-table"
                            hx-push-url="true">
                        Reset
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Pages Table -->
<div class="card">
    <div class="card-body p-0" id="pages-table">
        {% include "cms/partials/pages_table.html" %}
    </div>
</div>

<!-- Bulk Actions -->
<div class="card mt-3" id="bulk-actions" style="display: none;">
    <div class="card-body d-flex align-items-center gap-3">
        <span id="selected-count">0</span> selected
        <select class="form-select form-select-sm" style="width: auto;" id="bulk-action">
            <option value="">Bulk Actions</option>
            <option value="publish">Publish</option>
            <option value="unpublish">Unpublish</option>
            <option value="delete">Delete</option>
        </select>
        <button class="btn btn-sm btn-primary"
                hx-post="{% url 'cms:bulk_action' %}"
                hx-include="#bulk-action,.page-checkbox:checked"
                hx-target="#pages-table"
                hx-confirm="Apply this action to selected pages?">
            Apply
        </button>
    </div>
</div>

<script>
document.body.addEventListener('htmx:afterSwap', function(e) {
    if (e.detail.target.id === 'pages-table') {
        setupCheckboxes();
    }
});

function setupCheckboxes() {
    var selectAll = document.getElementById('select-all');
    if (selectAll) {
        selectAll.addEventListener('change', function() {
            document.querySelectorAll('.page-checkbox').forEach(cb => cb.checked = this.checked);
            updateBulkActions();
        });
    }

    document.querySelectorAll('.page-checkbox').forEach(cb => {
        cb.addEventListener('change', updateBulkActions);
    });
}

function updateBulkActions() {
    var checked = document.querySelectorAll('.page-checkbox:checked').length;
    document.getElementById('selected-count').textContent = checked;
    document.getElementById('bulk-actions').style.display = checked > 0 ? 'block' : 'none';
}

setupCheckboxes();
</script>
{% endblock %}
{% endraw %}
