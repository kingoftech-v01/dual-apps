"""
CMS URL configuration for {{ app_name }} app.
Generated by dual-apps v{{ version }}
"""

from django.urls import path, re_path
from . import views_frontend

app_name = "{{ app_name }}"

urlpatterns = [
    path("", views_frontend.page_list, name="list"),
    path("pages/", views_frontend.page_list, name="page_list"),
    path("page/<slug:slug>/", views_frontend.page_detail, name="page_detail"),
    re_path(r"^(?P<path>.*)$", views_frontend.page_by_path, name="page_by_path"),
]
