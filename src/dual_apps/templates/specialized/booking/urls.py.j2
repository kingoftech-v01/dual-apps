"""
Booking URL configuration for {{ app_name }} app.
Generated by dual-apps v{{ version }}
"""

from django.urls import path
from . import views_frontend

app_name = "{{ app_name }}"

urlpatterns = [
    path("", views_frontend.service_list, name="list"),
    path("categories/", views_frontend.category_list, name="category_list"),
    path("category/<slug:category_slug>/", views_frontend.service_list, name="category_services"),
    path("services/", views_frontend.service_list, name="service_list"),
    path("service/<slug:slug>/", views_frontend.service_detail, name="service_detail"),
    path("book/", views_frontend.booking_create, name="booking_create"),
    path("my-bookings/", views_frontend.booking_list, name="booking_list"),
    path("booking/<uuid:pk>/", views_frontend.booking_detail, name="booking_detail"),
]
