"""
Django app configuration for {{ app_name }}.

Generated by dual-apps v{{ version }}
"""

from django.apps import AppConfig


class {{ app_name_pascal }}Config(AppConfig):
    """
    Configuration for the {{ app_name }} app.

    Features:
    - Dual layer architecture (Frontend + API)
    - UUID primary keys
    - OWASP security ready
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = '{{ app_name }}'
    verbose_name = '{{ app_name_title }}'

    def ready(self):
        """
        App initialization.

        Import signals and register plugins here.
        """
        # Import signals for post_save hooks
        try:
            from {{ app_name }} import signals  # noqa: F401
        except ImportError:
            pass
